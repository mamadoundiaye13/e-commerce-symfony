{% extends 'base.html.twig' %}

{% block title %}Edit Commande{% endblock %}

{% block body %}
    <div class="bg-light p-3">
        <h1>Edit Commande</h1>
    </div>

    <div class="row">
        <div class="col-lg-12">
            <h3 class="text-left">{{ include('panier/_form.html.twig', {'button_label': 'Update'}) }}</h3>
        </div>

    </div>
    <a href="{{ path('panier_commande') }}">back to list</a>
    <h3 class="text-right">{{ include('panier/_delete_form.html.twig') }}</h3>

{% endblock %}
